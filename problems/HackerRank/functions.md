# List of all important functions that can be used in Python problem solving


`collections.Counter()`
- A  counter is a container that stores elements as dictonary keys, and their counts are stored as dictonary values
- `{keys:values(counts)}`
